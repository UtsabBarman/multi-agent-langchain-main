# App DB (SQLite)
SQLITE_APP_PATH=./data/app.sqlite

# Connected SQLite DBs for tools (e.g. manufacturing)
SQLITE_MANUFACTURING_PATH=./data/manufacturing.sqlite

# Chroma: path for persistent store (directory)
CHROMA_PATH=./data/chroma

# LLM (OpenAI)
OPENAI_API_KEY=sk-...

# Orchestrator base URL (for gateway / Streamlit)
ORCHESTRATOR_BASE_URL=http://127.0.0.1:8000
